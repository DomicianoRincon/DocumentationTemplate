// src/content/config.js
// SPEC-10: Single build-time artifact that points the app at its content source.
//
// FOUNDING A COURSE: after pushing your own `toc.md` + `content/` to this
// repo's root, set tocUrl to your repo's raw URL:
//   https://raw.githubusercontent.com/<your-github-user>/<this-repo>/refs/heads/main/toc.md

const courseConfig = {
  tocUrl: 'REPLACE_WITH_YOUR_RAW_TOC_URL',
};

export default courseConfig;
